// metadata.ts

import type { Metadata } from 'next'
import './firebaseInit'; // adjust the path accordingly

export const metadata: Metadata = {
  title: 'FalconShop',
  description: 'Created by the Havoc Development Team',
}
